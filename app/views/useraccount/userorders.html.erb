<div class="row justify-content-center">
    <%= render 'useraccount/useraccountnav'%> 
</div>


<div class="row justify-content-center">
<% if @orders %>

<h1><%=current_user.username%>'s Orders</h1>
</div>
<div class="row justify-content-center">
<table>
  <thead>
    <tr>
      <th>Order date</th>
      <th>User</th>
      <th>Status</th>
      <th>Total</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.order_date %></td>
        <td><%= order.status %></td>
        <td><%= order.order_total %></td>
        <td><%= link_to '/useraccount/userorders/', order %></td>
        
       

      </tr>
    <% end %>
  </tbody>
</table>
<% end %>
</div>